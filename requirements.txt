# Core
numpy==1.26.2
pandas==2.1.3
scipy==1.17.1
scikit-learn==1.9.0
cvxpy==1.9.2
numba==0.67.0

# Data
tushare==1.4.29
baostock==0.9.3
akshare==1.18.94
tables==3.11.1
sqlalchemy==2.0.52
asyncpg==0.31.0

# Visualization
matplotlib==3.11.1
seaborn==0.13.2

# Web
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
python-multipart==0.0.32
websocket-client==1.9.2
httpx==0.28.1

# ML
# xgboost-cpu, not xgboost: the plain PyPI wheel hard-depends on
# nvidia-nccl-cu12 (342 MB) on Linux x86_64. This platform is CPU-only, so
# that dependency can never be used — it just bloats the Docker image and
# adds ~350 MB of download to every CI install. Same version, same `xgboost`
# module, same API.
xgboost-cpu==2.1.4
lightgbm==4.7.0
shap==0.51.0

# Config
pyyaml==6.0.3
python-dotenv==1.2.3

# Testing
pytest==7.4.3
pytest-cov==7.1.0
pytest-asyncio==0.21.2

# Compliance export
openpyxl==3.1.5
adata>=1.0.0
